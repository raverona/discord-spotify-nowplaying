# discord-spotify-nowplaying

This project is now obsolete as Discord has this function natively

## Initialization

`npm install`

Create a config.json to supply your discord `token`.

```json
{
	"token": "your-token-goes-here" 
}
```

## Running
`node bot.js`
